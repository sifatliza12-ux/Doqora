# components/ui

UI primitives (button, input, card, badge, etc.) built on the Design System v2.0 tokens in `src/app/globals.css` / `src/lib/design-tokens.ts`. Empty placeholder — populated starting Stage 1.
